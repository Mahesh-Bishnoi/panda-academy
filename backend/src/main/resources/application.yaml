spring:
  application:
    name: panda-academy
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:panda_academy.sqlite
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update